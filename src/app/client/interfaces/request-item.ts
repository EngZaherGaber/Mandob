export interface RequestItem {
  requestItemID: number;
  requestID: number;
  variantID: number;
  variantName: string;
  quantity: number;
  originalPrice: number;
  totalFinalPrice: number;
  finalPrice: number;
  isAvailable: boolean;
}
